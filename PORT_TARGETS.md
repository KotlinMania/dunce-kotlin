# Port targets — `dunce-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `dunce` that codex-rs uses, and therefore the minimum subset that `dunce-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-46] (rank `51`, tier `direct`, status `To Do`)

**Cargo package name**: `dunce`
**Rust module name (in `use` statements)**: `dunce`

## Summary

- **1** distinct symbols imported by codex-rs
- **7** total `use` statements
- **7** codex source files affected
- **5** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `dunce::canonicalize`

- **7 `use` site(s)** across **5 codex member crate(s)**: `codex-config`, `codex-core`, `codex-core-skills`, `codex-sandboxing`, `codex-windows-sandbox`
- Aliases used: `as canonicalize_path`, `as normalize_path`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `config/src/loader/mod.rs:36` (`codex-config`) — `use dunce::canonicalize as normalize_path`
- `core-skills/src/loader_tests.rs:13` (`codex-core-skills`) — `use dunce::canonicalize as canonicalize_path`
- `core/src/agents_md.rs:28` (`codex-core`) — `use dunce::canonicalize as normalize_path`
- `core/tests/suite/client.rs:67` (`codex-core`) — `use dunce::canonicalize as normalize_path`
- `sandboxing/src/manager_tests.rs:19` (`codex-sandboxing`) — `use dunce::canonicalize`
- `sandboxing/src/policy_transforms_tests.rs:16` (`codex-sandboxing`) — `use dunce::canonicalize`
- `windows-sandbox-rs/src/allow.rs:2` (`codex-windows-sandbox`) — `use dunce::canonicalize`
